import { FlatpickrDirective } from './flatpickr.directive';

describe('FlatpickrDirective', () => {
  it('should create an instance', () => {
    const directive = new FlatpickrDirective();
    expect(directive).toBeTruthy();
  });
});
